const Arrow = require('arrow');
const config = require( './config.js' );
const serverFactory = require('./serverFactory.js')(config, addSampleModeAndAPI);

function addSampleModeAndAPI(err, server) {
	console.log("fdjshfjkdshfjdkshfkjsh");
}
